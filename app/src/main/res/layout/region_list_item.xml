<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@android:color/transparent"
    android:paddingHorizontal="16dp"
    android:paddingVertical="8dp">

    <RelativeLayout
        android:id="@+id/flag_image_r"
        android:layout_width="match_parent"
        android:layout_height="80dp"
        android:background="@drawable/bg_server_item"
        android:padding="12dp">

        <!-- Flag Icon -->
        <com.google.android.material.card.MaterialCardView
            android:id="@+id/card_flag"
            android:layout_width="55dp"
            android:layout_height="40dp"
            android:layout_centerVertical="true"
            app:cardCornerRadius="6dp"
            app:cardElevation="0dp"
            app:strokeWidth="1dp"
            app:strokeColor="@color/glass_white_border">

            <ImageView
                android:id="@+id/country_flag"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:scaleType="centerCrop"
                android:src="@drawable/flag_default" />
        </com.google.android.material.card.MaterialCardView>

        <!-- Right Side Container (Ping + Crown) -->
        <LinearLayout
            android:id="@+id/right_info_container"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentEnd="true"
            android:layout_centerVertical="true"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <View
                android:id="@+id/signal_dot"
                android:layout_width="8dp"
                android:layout_height="8dp"
                android:layout_marginEnd="6dp"
                android:background="@drawable/bg_signal_dot" />

            <TextView
                android:id="@+id/region_limit"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginEnd="12dp"
                android:minWidth="40dp"
                android:gravity="center"
                android:text="---"
                android:textColor="@color/neon_green"
                android:textSize="12sp"
                android:textStyle="bold" />

            <ImageView
                android:id="@+id/crown_icon"
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:src="@drawable/ic_crown"
                app:tint="@color/text_color"
                android:visibility="visible" />

        </LinearLayout>

        <!-- Server Title (Centered between Flag and Right Info) -->
        <TextView
            android:id="@+id/region_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_centerVertical="true"
            android:layout_marginStart="16dp"
            android:layout_marginEnd="8dp"
            android:layout_toStartOf="@id/right_info_container"
            android:layout_toEndOf="@id/card_flag"
            android:ellipsize="end"
            android:maxLines="1"
            android:text="India (Mumbai) Optimized"
            android:textColor="@color/white"
            android:textSize="16sp"
            android:textStyle="bold" />

    </RelativeLayout>

    <!-- Deprecated hidden layouts to prevent R file desync -->
    <LinearLayout android:visibility="gone" android:layout_width="0dp" android:layout_height="0dp">
        <ImageView android:id="@+id/country_flag1" android:layout_width="0dp" android:layout_height="0dp" />
        <TextView android:id="@+id/region_title1" android:layout_width="0dp" android:layout_height="0dp" />
        <ImageView android:id="@+id/region_limit1" android:layout_width="0dp" android:layout_height="0dp" />
    </LinearLayout>

</RelativeLayout>
